id <- c(1, 2, 3)
ime <- c("Belma", "Selma", "Alma")
it_ak <- c("predavac", "polaznik", "polaznik")

df <- data.frame(id, ime, it_ak)

df2 <- data.frame(
  id = c(1, 2, 3),
  ime = c("Belma", "Selma", "Alma"),
  it_ak = c("predavac", "polaznik", "polaznik")
)
